# DON BEAT 管理ページからの曲登録

初回設定:
1. Cloudflare R2に保存用バケットを作成（例 donbeat-songs）。
2. Pagesプロジェクトの Settings > Bindings > Add > R2 bucket で、変数名 DONBEAT_BUCKET として接続。
3. 再デプロイ。既存ADMIN_TOKENを認証に利用します。バケット自体の公開設定は不要です。

/admin/に既存管理者キーでログインし「DON BEAT 譜面アップロード」を開きます。
ZIP/MCZ/フォルダ/複数ファイルに対応。ZIPはブラウザ内で展開し、参照される譜面・音源・動画だけ順番に転送します。
1ファイル90MiB以下、ZIPは既存展開処理の256MiB制限。転送進捗を表示し、通信失敗時は各ファイルを最大3回試行します。
全ファイルを保存してから曲一覧へ公開します。公開後はゲーム再読み込みで表示され、サイトの再デプロイは不要です。
MC level、全6種類のギミック表示にも対応します。
R2が未接続の場合はフォームに接続手順を表示し、ランキング機能はそのまま利用できます。
公開前のファイルには一般プレイヤーからアクセスできません。途中失敗時は未公開データの削除を試みます。
ブラウザを強制終了した場合の未公開データはR2のfiles/配下に残ることがあります。
同じ曲を再登録すると別登録になります。削除/差し替え管理UIは今回の対象外です。
