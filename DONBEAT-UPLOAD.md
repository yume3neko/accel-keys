# DONBEAT 専用管理画面

DONBEATの収録曲アップロード・曲の分類とジャンル変更・差し替え・削除、および実装段位の新規作成・編集・削除は、ACCEL KEYS ADMINから分離し、DONBEAT専用画面で管理します。

## 初回設定（Cloudflare Pages）

1. R2バケット `donbeat-songs` を作成し、Pagesプロジェクトに `DONBEAT_BUCKET` という名前でバインドします。既に登録されている場合はそのまま利用できます。
2. Pagesの本番環境「Settings → Variables and Secrets」に `DONBEAT_ADMIN_TOKEN` をSecretとして登録します。推測されにくい32文字以上のランダムな値を推奨します。
3. `main` の最新バージョンをデプロイします。PagesのGitHub連携が有効なら、mainへの更新で自動デプロイされます。
4. `/donbeat/upload.html` へアクセスすると、未ログインの場合は `/donbeat/admin-login.html` にリダイレクトされます。そこで1回だけ管理者キーを入力します。

認証はHttpOnly・Secure・SameSite=Strictの署名済みCookieで管理します。セッションはログインから8時間有効で、ログアウトもできます。曲のアップロード、管理、段位編集のたびに管理者トークンを再入力する必要はありません。ACCEL KEYSの管理者キー `ADMIN_TOKEN` はDONBEATには使用しません。

## 譜面・音源のアップロード

管理画面の「譜面アップロード」でジャンル名を含むフォルダを選択します。例：

```text
本家譜面/
└─ ゲームミュージック/
   └─ 曲名/
      ├─ 譜面.tja
      └─ 音源.ogg
```

TJAの `WAVE:` とMalody太鼓モードMCのBGM `sound` を使って音源を照合します。指定がなければ譜面と同じフォルダの同名音源または唯一の音源を使います。ファイルはまず非公開のR2プレフィックスにアップロードし、サーバーが全ファイルの存在・ファイルサイズ・譜面・音源の対応を再確認します。**1曲でも不備があればカタログを作らず、全曲を非公開のままにします。** 全曲がそろった場合だけカタログを1回で作成し、一括で公開します。

対応形式：TJA/MC、音源はOGG・MP3・WAV・M4A・FLAC・OPUS・AAC、素材はMP4・WEBM・M4V・PNG・JPG・WEBP・GIFです。現行の専用アップローダーは解凍済みフォルダに対応します。ZIP/MCZは事前に解凍してください。1ファイル90MiB以下、譜面1ファイル8MiB以下、1回1200ファイル・合計4GiB以下です。

## 収録曲・段位の管理

同じ専用画面内の「収録曲・段位管理」で、曲の分類（本家／創作）、本家譜面のジャンル、曲の差し替え・削除ができます。差し替え時は新しい一組の譜面と音源を検証して公開した**後**に、元の登録曲を削除します。公開済みの曲が段位に使われている場合、削除の影響に注意してください。

段位は `.dan` ファイルまたは画面内の作成フォームから登録できます。R2に保存された段位は設定ファイルを再編集でき、静的な組み込み段位は非表示・再表示できます。

Cloudflare Pagesの再デプロイは、公開された曲・R2段位の追加時には不要です。ゲームの曲一覧を再読み込みしてください。バケット自体のパブリック公開設定は不要です。
