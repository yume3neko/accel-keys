# ACCEL KEYS マルチプレイ

入口: `/accel-keys/multiplayer.html`（ソロ画面下部にもリンク）。

- 2〜4人。ルーム番号または招待リンクで集合し、全員が準備完了後にホストが開始。
- 通常 / 加速する大宇宙 × サバイバルバトル / 協力の4通り。
- サバイバルは各自4ミスで脱落。残り1人以下になった後、2.5秒の結果受信猶予を経て勝敗を確定。全員脱落は引き分け。
- 協力は人数×4の共有ミス枠。個人で4ミスしても共有枠が残っていれば続行。
- 協力中の退出・15秒以上の通信切断は協力終了。バトルでは脱落。待機中は退出したホストを次の参加者が引き継ぐ。
- ページを閉じる・移動する操作は退出。通信だけの一時切断は15秒以内なら再接続を試行。
- シードと判定予定時刻を共有。通常モードも全員同じ配置。HSと画面寸法で判定時刻は変わらない。
- 判定は端末内で行う。PERFECT ±60ms / GREAT ±110ms / GOOD ±170ms。HSによる得点補正なし。
- 通常の倍率は8まで、密度は無制限。大宇宙は倍率・密度とも無制限。
- マルチの記録はソロランキングと分離し、今回の結果画面のみで表示。

## 配置

既存Cloudflare Pagesの`DB`バインディングをそのまま使用する。追加のWorker・Durable Objects設定は不要。
`functions/api/multiplayer.js`が初回アクセス時に`multiplayer_rooms`テーブルを作成する。
既存のスコアテーブルは変更しない。ルームは最後の操作から2時間で期限切れになる。

ルーム状態は約900ms間隔のPOSTで同期（通信時間を含む実際の更新間隔はこれより長い）。
同時更新はバージョン条件付きUPDATEと再試行で保護する。接続ごとの参加トークンはsessionStorageで保持し、他プレイヤーへの応答には含めない。
ホスト以外の開始、未準備での開始、満員・開始済みルームへの新規参加、他ルームの対戦情報、古い連番や逆行するカウンターは拒否または無視する。

フレンド対戦向け。判定・得点申告は端末を信頼するため、競技ランキング向けの完全な不正検証は行っていない。
通信遅延は相手表示・勝敗確定の遅れに影響する。大人数・大規模運用ではWebSocketなどへの移行を検討する。

## 検証

Node.js 22.13以上（node:sqliteを利用）:

```sh
node --test tests/multiplayer*.test.mjs
```

SQLite上のAPI統合、4組み合わせの進行、同時参加・同時報告、認証・準備・定員、切断・引き分けを検証する。
クライアント統合テストは最小DOM環境で実際の画面スクリプトとAPIを接続している。実機の描画・タッチ操作・公衆回線の遅延を再現するものではない。


## チーム戦・特殊ノーツ

- 通常／大宇宙の両譜面で `team`（2対2）を選択できます。待機画面でA/Bを2人ずつ選択します。各自が自分のチームを変更でき、ホストはCPUを含む全員を配置できます。全員の準備完了が必要です。
- 各自HP4、チームHPは2人の残りHPの合計です。HP0でもプレイ・特殊ノーツ・味方の回復を続行できますが、HP0の人は回復対象外です。チーム全員のHP0で敗北。退出者はHP0扱いです。
- ホストは待機画面で特殊ノーツをON/OFFできます（初期OFF）。設定やチーム変更時は人間の準備完了を解除します。個人戦・協力でも特殊ノーツを使用できます。
- 特殊ノーツは譜面seedとノーツIDから約10%で決定される金色の★ノーツです。取得時、サーバーが適用可能な効果から1つを等確率で選びます。
- おじゃま：ランダムな対戦相手1人に、下部50%のマスク／上部40%のマスク／判定幅を65%にする妨害のいずれかを5〜10秒付与。協力モードでは抽選しません。
- シールド：5秒間ミスによるHP減少・コンボ切断を防ぎます。判定強化：10秒間、各判定幅を150%にします。判定妨害と重なる場合は倍率を掛け合わせます。同じ効果の再取得は期限を更新します。
- 回復：チーム戦のみ、生存中かつHP4未満の味方1人を1回復。対象がいない場合は回復を抽選しません。
- チャレンジ：発動後の100連続ヒットで15秒オート。途中のミスで終了（シールド中は失敗しません）。チャレンジ中は特殊ノーツを通常ノーツとして表示・処理し、100コンボ目も特殊効果を抽選しません。オートでも特殊ノーツを取得できます。
- 効果・HP・チャレンジはサーバー管理。判定はノーツIDと連番で再送可能にし、重複取得を拒否します。サーバーが同じ譜面を再生してノーツ時刻を照合します。クライアント判定・得点を信頼する基本設計は従来どおりで、完全な不正対策ではありません。効果の共有には通常のポーリング通信分の遅延があります。
- デバッグCPUもチーム配置・特殊効果に対応します。判定妨害／強化中はCPUの基準ミス率をそれぞれ1.6倍／0.5倍にします。

検証：`node --test tests/*.test.mjs`（Node 24以上、node:sqlite使用）。

## ルームチャットと管理コマンド

待機画面の「チャット」からのみ送信できます（コマンドも同じ制限）。1件200文字まで、最新60件をルームに保存し、既存のポーリングで共有します。試合開始時はチャットと新着通知を閉じます。新着メッセージはチャットボタン横に4秒間表示します。画面内のコマンド説明は非表示です。送信IDによる重複防止、500msの送信間隔制限、テキストとしての描画を行います。

コマンドはホストまたは管理者パスワードで認証済みの製作者のみ実行可能です。権限はサーバー保存の参加者情報で検証します。半角スペースで区切り、対象名は表示名をそのまま指定してください（CPU 1 のような空白入りの名前にも対応）。同名が複数いる場合は誤操作防止のため拒否します。

| コマンド例 | 動作 |
| --- | --- |
| `/auto CPU 1 True` | 対象のオートを有効化。自動入力・全PERFECT。Falseで解除。共有チャットへの記録・通知なし。 |
| `/bot 2 3 5` | レベル3と5のBOTを2人追加。通常ルームでも使用可能。 |
| `/kick 名前` / `/ban 名前` | 対象を退出。恒久BANではなく同じ動作。自分自身は対象外。 |
| `/mode` | 実行者にモード変更画面を表示。譜面・個人戦／チーム戦／協力・特殊ノーツを変更可能。 |

BOT追加とモード変更は開始前のみ。BOTを含めルーム上限4人、BOTレベルは1〜5で人数分指定します。レベル3は従来の精度・ミス率、レベル5はミスなしの全PERFECTです。レベル1・2・4の基準ミス率はそれぞれ25〜35%・15〜20%・1〜3%で、ヒット時のPERFECT比率も段階的に増えます。

モード変更後は人間の準備完了を解除します。遊び方を変更した場合はチーム選択も解除します。autoの設定はそのルームへの参加中に維持されますが、脱落・退出した人の復活は行いません。auto以外のコマンドは操作結果を共有チャットに記録します。

上部・下部妨害中の対象には、上部と下部の両方を追加抽選しません（同種の延長もなし）。ノイズは抽選・描画から削除しました。
