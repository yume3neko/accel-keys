# ACCEL KEYS マルチプレイ

入口: `/accel-keys/multiplayer.html`（ソロ画面下部にもリンク）。

- 2〜4人。ルーム番号または招待リンクで集合し、全員が準備完了後にホストが開始。
- 通常 / 加速する大宇宙 × サバイバルバトル / 協力の4通り。
- サバイバルは各自4ミスで脱落。残り1人以下になった後、2.5秒の結果受信猶予を経て勝敗を確定。全員脱落は引き分け。
- 協力は人数×4の共有ミス枠。個人で4ミスしても共有枠が残っていれば続行。
- 協力中の退出・15秒以上の通信切断は協力終了。バトルでは脱落。待機中は退出したホストを次の参加者が引き継ぐ。
- ページを閉じる・移動する操作は退出。通信だけの一時切断は15秒以内なら再接続を試行。
- シードと判定予定時刻を共有。通常モードも全員同じ配置。HSと画面寸法で判定時刻は変わらない。
- 判定は端末内で行う。PERFECT ±60ms / GREAT ±110ms / GOOD ±170ms。HSによる得点補正なし。
- 通常の倍率は8まで、密度は無制限。大宇宙は倍率・密度とも無制限。
- マルチの記録はソロランキングと分離し、今回の結果画面のみで表示。

## 配置

既存Cloudflare Pagesの`DB`バインディングをそのまま使用する。追加のWorker・Durable Objects設定は不要。
`functions/api/multiplayer.js`が初回アクセス時に`multiplayer_rooms`テーブルを作成する。
既存のスコアテーブルは変更しない。ルームは最後の操作から2時間で期限切れになる。

ルーム状態は約900ms間隔のPOSTで同期（通信時間を含む実際の更新間隔はこれより長い）。
同時更新はバージョン条件付きUPDATEと再試行で保護する。接続ごとの参加トークンはsessionStorageで保持し、他プレイヤーへの応答には含めない。
ホスト以外の開始、未準備での開始、満員・開始済みルームへの新規参加、他ルームの対戦情報、古い連番や逆行するカウンターは拒否または無視する。

フレンド対戦向け。判定・得点申告は端末を信頼するため、競技ランキング向けの完全な不正検証は行っていない。
通信遅延は相手表示・勝敗確定の遅れに影響する。大人数・大規模運用ではWebSocketなどへの移行を検討する。

## 検証

Node.js 22.13以上（node:sqliteを利用）:

```sh
node --test tests/multiplayer*.test.mjs
```

SQLite上のAPI統合、4組み合わせの進行、同時参加・同時報告、認証・準備・定員、切断・引き分けを検証する。
クライアント統合テストは最小DOM環境で実際の画面スクリプトとAPIを接続している。実機の描画・タッチ操作・公衆回線の遅延を再現するものではない。
