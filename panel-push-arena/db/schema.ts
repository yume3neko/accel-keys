import { integer,sqliteTable,text } from "drizzle-orm/sqlite-core";
export const rooms=sqliteTable("rooms",{code:text("code").primaryKey(),hostId:text("host_id").notNull(),status:text("status").notNull().default("waiting"),duration:integer("duration").notNull().default(60),targets:integer("targets").notNull().default(4),difficulty:text("difficulty").notNull().default("normal"),seed:integer("seed").notNull(),createdAt:integer("created_at").notNull(),startedAt:integer("started_at"),endsAt:integer("ends_at")});
export const players=sqliteTable("players",{id:text("id").primaryKey(),roomCode:text("room_code").notNull().references(()=>rooms.code),name:text("name").notNull(),score:integer("score").notNull().default(0),combo:integer("combo").notNull().default(0),bestCombo:integer("best_combo").notNull().default(0),currentRound:integer("current_round").notNull().default(0),pressed:text("pressed").notNull().default("[]"),mistakes:integer("mistakes").notNull().default(0),perfects:integer("perfects").notNull().default(0),joinedAt:integer("joined_at").notNull(),lastSeen:integer("last_seen").notNull()});
export const messages=sqliteTable("messages",{id:integer("id").primaryKey({autoIncrement:true}),roomCode:text("room_code").notNull().references(()=>rooms.code),playerId:text("player_id").notNull(),name:text("name").notNull(),body:text("body").notNull(),createdAt:integer("created_at").notNull()});
